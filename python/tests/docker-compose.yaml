version: '3.7'

services:
  scylla:
    image: scylladb/scylla:5.2
    ports:
      - 9042:9042
    healthcheck:
      test: ["CMD", "cqlsh", "-e", "SELECT * FROM system.local"]
      interval: 5s
      timeout: 5s
      retries: 60
      start_period: 15s